{
  "name": "canvas-sync",
  "version": "1.0.0",
  "description": "Command line program to sync files from canvas",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "run": "babel src -d dist && node dist/index.js"
  },
  "author": "Michael Fine",
  "license": "ISC",
  "dependencies": {
    "lodash": "^4.17.4",
    "request-promise": "^4.2.1"
  },
  "devDependencies": {
    "babel-loader": "^7.1.2",
    "babel-preset-env": "^1.6.0",
    "babel-preset-es2015": "^6.24.1",
    "babel-watch": "^2.0.7"
  }
}
